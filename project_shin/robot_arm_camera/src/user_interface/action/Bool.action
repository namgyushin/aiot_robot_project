# Goal: 클라이언트가 서버에 전달하려는 목표
# 여기에 목표를 나타내는 메시지가 들어갑니다.
bool goal_value  # 목표 값 (True/False)

---

# Result: 서버가 클라이언트에게 응답하는 결과 메시지
# 목표 실행 후, 서버가 응답하는 값
bool success  # 실행 성공 여부

---

# Feedback: 서버가 실행 중에 클라이언트에게 전달하는 피드백
# 피드백이 필요한 경우 사용
string message  # 피드백 메시지
